/**
*main - Serves as the entry point for the program
*
*Return: By default returns 0 if errors otherwise returns non-zer value
*/
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
