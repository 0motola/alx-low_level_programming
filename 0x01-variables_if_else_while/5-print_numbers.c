#include <stdio.h>

/**
*main- prints a random number and states if it is poositive, negative or zero.
*
*Return: always 0 (success)
*/
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}

	printf("\n");

	return (0);
}

